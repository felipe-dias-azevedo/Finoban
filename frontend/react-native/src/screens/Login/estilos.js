import React from 'react';
import {
    StyleSheet
} from 'react-native';

export default StyleSheet.create({
    containerLogin: {
        flex: 1,
        flexDirection: 'column',
        justifyContent: 'center',
        alignItems: 'center',
    },
    textHeader: {
        fontSize: 30,
        fontWeight: 'bold',
        marginBottom: 100,
    },
    forgotPassword: {
        marginTop: 15,
        alignItems: 'flex-end',
    },
    registerAccount: {
        marginTop: 10,
    },
    bold: {
        fontWeight: 'bold',
    }
})